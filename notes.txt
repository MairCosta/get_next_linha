int	ft_clnbuf(char *buf)
{
	int	i;
	int	j;
	int	newln;

	i = 0;
	j = 0;
	newln = 0;
	if (!buf)
		return (0);
	while (buf[i])
	{
		if (newln)
			buf[j++] = buf[i];
		if (buf[i] == '\n')
			newln = 1;
		buf[i++] = '\0';
	}
	return (newln);
}
